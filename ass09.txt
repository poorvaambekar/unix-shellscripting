Write a script intended to iterate through an array called SERVERLIST containing at least four values (server names).
 Display all four values to the terminal when run.


===
SERVERLIST=("abc" "def" "pqr" "lmn")
echo "${SERVERLIST[0]}"
echo "${SERVERLIST[1]}"
echo "${SERVERLIST[2]}"
echo "${SERVERLIST[3]}"
echo "${SERVERLIST[0]}"